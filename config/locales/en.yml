en:
  banana: asd
  maintenance_statuses:
    active: In Progress
    closed: Finished
    upcoming: Upcoming

  service_status_types:
    ok: OK - everything is working as it should be
    minor: Minor Issue - there are some issues but not affecting the whole service
    major: Major Issues - a complete service outage affecting all users
    maintenance: Maintenance - the service is currently undergoing planned maintenance

  default_email_subjects:
    subscribed: "Confirm your subscription to {{site.title}}"
    new_issue: "[New Issue] {{issue.title}} on {{site.title}}"
    new_issue_update: "[Updated Issue] {{issue.title}} on {{site.title}}"
    new_maintenance: "[Maintenance] {{maintenance.title}} on {{site.title}}"
    new_maintenance_update: "[Maintenance Update] {{maintenance.title}} on {{site.title}}"

  activerecord:
    attributes:
      maintenance:
        length_in_minutes: Session length
